/*
 * This file is part of Moodle - http://moodle.org/
 *
 * Moodle is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 *
 * Moodle is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 * GNU General Public License for more details.
 *
 * You should have received a copy of the GNU General Public License
 * along with Moodle.  If not, see <http://www.gnu.org/licenses/>.
 *
 * HAND-AUTHORED FALLBACK BUILD - see amd/src/shortcuts.js for the
 * annotated ES module source and INSTALL.md for why this file (and its
 * .map) exist.
 */
define([], function() {

    var isTypingTarget = function(target) {
        if (!target) {
            return false;
        }
        var tag = target.tagName ? target.tagName.toLowerCase() : '';
        return tag === 'input' || tag === 'textarea' || tag === 'select' || target.isContentEditable === true;
    };

    var bind = function(handlers, enabled) {
        var active = (enabled === undefined) ? true : enabled;

        var normalise = function(key) {
            return key.length === 1 ? key.toLowerCase() : key;
        };

        var normalisedHandlers = {};
        Object.keys(handlers).forEach(function(key) {
            normalisedHandlers[normalise(key)] = handlers[key];
        });

        var listener = function(event) {
            if (!active || isTypingTarget(event.target)) {
                return;
            }

            var handler = normalisedHandlers[normalise(event.key)];
            if (handler) {
                event.preventDefault();
                handler(event);
            }
        };

        document.addEventListener('keydown', listener);

        return {
            setEnabled: function(value) {
                active = value;
            },
            destroy: function() {
                document.removeEventListener('keydown', listener);
            },
        };
    };

    return {bind: bind};
});
//# sourceMappingURL=shortcuts.min.js.map
